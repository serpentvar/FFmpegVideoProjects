# Remux Utilities

## Scripts

- `mkv_to_mp4_copy.bat`: Copy video and audio streams from MKV or other inputs into an MP4 container.
- `mp4_remux_fix.bat`: Rewrap an MP4 into a fresh MP4 container without re-encoding.

These scripts use stream copy, so they are fast and do not change video quality.
