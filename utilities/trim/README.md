# Trim Utilities

## Scripts

- `mp4_trim_copy.bat`: Cut video by timestamp while copying streams.
- `mp3_trim.bat`: Cut audio by timestamp and export MP3.

When prompted, enter timestamps in `hh:mm:ss` format.
